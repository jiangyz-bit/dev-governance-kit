# Local Runbook

## Prerequisites

- Mini program development tool
- Node.js for local scripts
- Backend service reachable from the configured environment

## Verify

```bash
{{INSTALL_COMMAND}}
{{TEST_COMMAND}}
node scripts/generate-status-registry.mjs
node scripts/check-status-registry.mjs
```

