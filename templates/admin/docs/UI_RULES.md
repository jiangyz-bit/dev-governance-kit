# UI Rules

- Build the actual operations tool, not a marketing page.
- Every data page should handle loading, empty, error, permission denied, and successful mutation states.
- Use domain constants for statuses and workflow helpers for action availability.
- Do not compare workflow status against ad hoc string literals in page components.
- Use backend data as the source of truth after mutations.

